services:
  - type: web
    name: bekfood
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production